T=1e5;
dt=.025;
tau_c=.0234;
C_0=-12.342;
C=C_0*ones(1,T);
A=1.234;
sig_c = .023;
n=round(rand(1,T)*.505*0);
e=randn(1,T);
var(sig_c*sqrt(dt)*e)
for t=1:T-1, C(t+1)=(1-dt/tau_c)*C(t)+C_0*dt/tau_c+A*n(t) + sig_c*sqrt(dt)*e(t); end
plot(C)
var(C)/dt